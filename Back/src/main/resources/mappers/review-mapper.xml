<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Investube.mvc.model.dao.ReviewDao">

	<!-- 전체 리뷰 조회 -->
	<select id="selectAll" resultType="Review">
		SELECT * FROM reviews
		ORDER BY created_at DESC
	</select>
	
	<!-- 리뷰 ID로 조회 -->
	<select id="selectOne" parameterType="int" resultType="Review">
		SELECT * FROM reviews
		WHERE review_id = #{reviewId}
	</select>
	
	<!-- 비디오별 리뷰 조회 -->
	<select id="selectByVideoId" parameterType="int" resultType="Review">
		SELECT * FROM reviews
		WHERE video_id = #{videoId}
		ORDER BY created_at DESC
	</select>
	
	<!-- 리뷰 등록 -->
	<insert id="insertReview" parameterType="Review">
		INSERT INTO reviews (video_id, user_id, content, rating, created_at)
		VALUES (#{videoId}, #{userId}, #{content}, #{rating}, NOW())
	</insert>
	
	<!-- 리뷰 수정 -->
	<update id="updateReview" parameterType="Review">
		UPDATE reviews
		SET content = #{content},
		    rating = #{rating},
		    updated_at = NOW()
		WHERE review_id = #{reviewId}
	</update>
	
	<!-- 리뷰 삭제 -->
	<delete id="deleteReview" parameterType="int">
		DELETE FROM reviews
		WHERE review_id = #{reviewId}
	</delete>
	
	<!-- 작성한 리뷰 조회 -->
	<select id="getReviewsByUser" resultType="Review">
	    SELECT *
	    FROM reviews
	    WHERE user_id = #{userId}
	    ORDER BY created_at DESC
	</select>
	

</mapper>
