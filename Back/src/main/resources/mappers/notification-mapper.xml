<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Investube.mvc.model.dao.NotificationDao">

  <resultMap id="NotificationResultMap" type="com.Investube.mvc.model.dto.Notification">
    <id property="id" column="id"/>
    <result property="recipientId" column="recipient_id"/>
    <result property="actorId" column="actor_id"/>
    <result property="type" column="type"/>
    <result property="targetType" column="target_type"/>
    <result property="targetId" column="target_id"/>
    <result property="message" column="message"/>
    <result property="isRead" column="is_read"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <insert id="insertNotification" parameterType="map" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO notifications (recipient_id, actor_id, type, target_type, target_id, message, is_read, created_at)
    VALUES (#{recipientId}, #{actorId}, #{type}, #{targetType}, #{targetId}, #{message}, FALSE, NOW())
  </insert>

  <select id="selectByRecipient" parameterType="map" resultMap="NotificationResultMap">
    SELECT * FROM notifications
    WHERE recipient_id = #{recipientId}
    ORDER BY created_at DESC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <select id="selectRecentByRecipient" parameterType="int" resultMap="NotificationResultMap">
    SELECT * FROM notifications
    WHERE recipient_id = #{recipientId}
    ORDER BY created_at DESC
    LIMIT 50
  </select>

  <select id="selectUnreadCount" parameterType="int" resultType="int">
    SELECT COUNT(*) FROM notifications WHERE recipient_id = #{recipientId} AND is_read = FALSE
  </select>

  <update id="markAsRead" parameterType="map">
    UPDATE notifications SET is_read = TRUE WHERE id = #{id} AND recipient_id = #{recipientId}
  </update>

  <update id="markAllAsRead" parameterType="int">
    UPDATE notifications SET is_read = TRUE WHERE recipient_id = #{recipientId}
  </update>

  <!-- notification_settings: per-user notification type on/off -->
  <select id="selectSettingsByUser" parameterType="int" resultType="map">
    SELECT user_id AS userId, type, enabled
    FROM notification_settings
    WHERE user_id = #{userId}
  </select>

  <select id="isNotificationEnabled" parameterType="map" resultType="boolean">
    SELECT enabled
    FROM notification_settings
    WHERE user_id = #{userId} AND type = #{type}
  </select>

  <insert id="upsertSetting" parameterType="map">
    INSERT INTO notification_settings (user_id, type, enabled)
    VALUES (#{userId}, #{type}, #{enabled})
    ON DUPLICATE KEY UPDATE enabled = VALUES(enabled)
  </insert>

</mapper>
